[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -q --strict-markers
markers =
    integration: 需要真实 MySQL / Chroma / bge 模型 / LLM API，默认跳过（设 RUN_INTEGRATION=1 才运行）
