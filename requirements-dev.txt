# Development environment: full dependency set plus test tooling.
#
# For offline tests only, requirements-ci.txt is much lighter
# (no torch / chromadb):
#   pip install -r requirements-ci.txt
-r requirements.txt
pytest>=8.0
pip-audit>=2.7

# NOTE: keep this file ASCII-only (see requirements.txt).
