# Minimal dependency set for offline tests and CI.
#
# Tests default to the fallback components (EMBEDDING_PROVIDER=hash /
# VECTOR_STORE=memory / LLM_PROVIDER=fake), so chromadb, sentence-transformers
# and torch are NOT required, and neither are MySQL or network access.
# .github/workflows/ci.yml installs exactly this file.
fastapi>=0.115
python-multipart>=0.0.9
python-dotenv>=1.0
httpx>=0.27
pypdf>=4.0
python-docx>=1.1
pymysql>=1.1
PyJWT>=2.8
pytest>=8.0
openpyxl>=3.1
alembic>=1.13
redis>=5.0

# NOTE: keep this file ASCII-only (see requirements.txt).
