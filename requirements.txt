# Web
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# Config & HTTP
python-dotenv>=1.0
httpx>=0.27

# Document parsing (.md is read as plain text, so the markdown lib is not needed)
pypdf>=4.0
python-docx>=1.1

# RAG defaults (heavy; the offline fallbacks hash / memory / fake do not need them)
chromadb>=0.5
sentence-transformers>=3.0

# Database
pymysql>=1.1

# Auth
PyJWT>=2.8

# NOTE: keep this file ASCII-only. pip-audit (pip_requirements_parser) decodes
# BOM-less files with the locale codec -- cp936 on a zh-CN Windows -- so non-ASCII
# comments raise UnicodeDecodeError and break dependency auditing.
