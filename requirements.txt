# Web
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# 配置与请求
python-dotenv>=1.0
httpx>=0.27

# 文档解析（.md 按纯文本读取，不需要 markdown 库）
pypdf>=4.0
python-docx>=1.1

# RAG（默认生产组件，较大；离线回退组件 hash / memory / fake 不需要它们）
chromadb>=0.5
sentence-transformers>=3.0

# 数据库
pymysql>=1.1

# 鉴权
PyJWT>=2.8
